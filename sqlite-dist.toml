[package]
name = "sqlite-graft"
authors = ["Carl Sverre"]
emails = ["hello@orbitinghail.dev"]
license = "MIT OR Apache"
description = "A SQLite extension which uses Graft to replicate to and from object storage."
homepage = "https://github.com/orbitinghail/graft"
repo = "https://github.com/orbitinghail/graft"
git_tag_format = "v$VERSION"

[targets]
npm = {}
pip = {}
datasette = {}
sqlite_utils = {}
gem = { module_name = "SqliteGraft" }
