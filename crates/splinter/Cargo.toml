[package]
name = "splinter"
version = "0.1.0"
edition = "2021"

[dependencies]
bytes = "1.7"
either = "1.13.0"
thiserror = "2.0"
zerocopy = { version = "0.8", features = ["derive"] }
itertools = "0.13"

[dev-dependencies]
criterion = { version = "0.5", features = ["html_reports"] }
roaring = "0.10"

[[bench]]
name = "bitmaps"
harness = false
